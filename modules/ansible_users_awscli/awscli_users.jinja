---
awscli_users:
%{ for name, data in users ~}
  - user: ${name}
    state: present
    profiles:
      - name: default
        aws_access_key_id: ${data.access_key}
        aws_secret_access_key: ${data.secret_access_key}
        region: ${data.region}
        output: yaml
%{ endfor ~}
